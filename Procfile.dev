web: bundle exec bin/rails s -p 3000
js: yarn build --watch
# css: yarn build:css --watch
# sidekiq: bundle exec sidekiq  # only if testing sidekiq workers
# karafka: bundle exec karafka s  # only if trying to consume kafka messages
